<div class="menu-tile">{{ collection.title }}  {{ collection.all_products_count }}</div>
<div id="products">
  {% for product in collection.products %}
		{% include 'product-body' %}
  {% endfor %}
</div>
<div id="color-target"></div>
